runners:
  local:
    # Default runner set to local.
    # The pipeline is architected for horizontal scalability using MapReduce.
    # To deploy on a real cluster, switch the runner to 'emr' or 'hadoop'
    # and provide the appropriate credentials and setup.
